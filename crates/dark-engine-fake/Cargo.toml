[package]
name = "dark-engine-fake"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "A scripted engine for tests and interface development."

# Keep this crate cheap to build. Seven task units test against it, so its
# build time lands on every one of them. See task unit B1.
[dependencies]
async-trait.workspace = true
dark-contract.workspace = true
futures-util.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["sync", "time"] }
tokio-util.workspace = true
toml.workspace = true

[dev-dependencies]
futures-util.workspace = true
tokio = { workspace = true, features = ["sync", "time", "rt", "macros"] }

[lints]
workspace = true
