[toolchain]
# Every contributor and every agent gets rustfmt and clippy without a setup
# step. The checks then behave the same everywhere.
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
